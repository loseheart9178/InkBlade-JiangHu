# GPT Image 2 Priority Queue

Generated by Wave 3C on 2026-05-03 and refreshed by Wave 5, Wave 6 CardArtQueue rescue, Wave 7 alpha-doc refresh, Wave 8 release handoff, Wave 9 polish/balance/art integration, and Wave 10 card fallback zero integration. This is a prompt and verification queue; Wave 9 and Wave 10 bound repo-local semantic SVG card faces and attack strips for runtime readability, not GPT Image 2 bitmap output.

## Baseline Audit

```bash
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe scripts/audit-generated-assets.mjs
```

Latest Wave 10 integration audit in `.worktrees/wave6-integration` on branch `codex/wave10-card-fallback-zero`:

- Runtime references: 159
- Missing files: 0
- Current `ink-pass` debt: 0
- Non-blocking card fallback debt: 0
- GPT2 runtime assets: 52
- Source sheets: 20
- Prompt queue targets: 54

Audit rerun in the integrated Wave 10 branch on 2026-05-05 passed with the counts above after all remaining card fallback targets were bound to semantic SVG card faces and the audit script was taught to count imported card-art modules.

## Card Fallback Ledger

`cardFallbackDebt` is now 0 at runtime. Wave 10 replaced the remaining shared type-level card bindings with repo-local semantic SVG files under `public/assets/generated/cards/`. These assets are readability-grade runtime art, not final GPT Image 2 bitmap generations; the prompt queue remains useful for later art-quality replacement.

Wave 10 status:

- Added and bound 45 semantic SVG card faces for common, ink, mind, status, Zhao Yun, Diao Chan, Cai Wenji, and Zhuge Liang fallback targets.
- Split runtime definitions into `src/game/content/cardArt/wave10CommonCardArt.ts`, `wave10ZhaoDiaoCardArt.ts`, and `wave10CaiZhugeCardArt.ts`, then imported them through `src/game/content/visuals.ts`.
- Updated `scripts/audit-generated-assets.mjs` so card-art module assets are included in runtime reference counts and card fallback debt calculation.
- `cardFallbackDebt` dropped from 45 to 0 after `scripts/audit-generated-assets.mjs`.

Wave 9 Task 2 status:

- Added and bound semantic SVG card faces for `zhao_strike`, `zhao_guard`, `zhao_longdan`, `diao_strike`, `diao_guard`, `diao_lingbo`, `cai_plain_strike`, `cai_pluck_string`, `cai_gong_tone`, `zhuge_fan_strike`, and `zhuge_guard`.
- These Wave 9 assets are repo-local SVG artwork with no visible text labels, not GPT Image 2 bitmap generations.
- `cardFallbackDebt` dropped from 56 to 45 after `scripts/audit-generated-assets.mjs`.

Wave 6 CardArtQueue rescue status:

- No GPT Image 2 generated source sheet or bitmap runtime crop exists in this worktree for the starter readability or common foundation card-art batches.
- Milestone 58 remains deferred as art backlog rather than hidden by binding fallback files to card ids.
- The executable JSON queue keeps the 35 historical targets and adds 19 deferred card-face targets for starter/common card ids, for 54 total prompt targets.
- Historical note: Wave 10 has now supplied semantic SVG runtime replacements for the remaining card fallback debt. GPT Image 2 bitmap replacement remains optional art-quality backlog.

The executable queue is stored at:

```text
public/assets/generated/gpt2-prompt-queue.json
```

Summary:

| Group | Count |
|---|---:|
| Total fallback cards | 0 |
| Missing dedicated `cardArtById` entries | 0 |
| Dedicated ids sharing type-level asset paths | 0 |

By character:

| Character | Count |
|---|---:|
| common | 0 |
| caiwenji | 0 |
| zhugeliang | 0 |
| zhaoyun | 0 |
| diaochan | 0 |

By type:

| Type | Count |
|---|---:|
| skill | 0 |
| attack | 0 |
| body | 0 |

By rarity:

| Rarity | Count |
|---|---:|
| common | 0 |
| uncommon | 0 |
| rare | 0 |
| ink | 0 |
| status | 0 |

Optional next GPT Image 2 art-quality batches:

1. Starter readability batch: runtime readability is covered by Wave 9 repo-local semantic SVGs for `zhao_guard`, `zhao_strike`, `zhao_longdan`, `diao_strike`, `diao_guard`, `diao_lingbo`, `cai_plain_strike`, `cai_pluck_string`, `cai_gong_tone`, `zhuge_fan_strike`, and `zhuge_guard`; keep GPT Image 2 bitmap replacement only as optional art-quality backlog.
2. Wave 10 common foundation bitmap pass: `common_pifeng`, `common_duanzhu`, `common_gedang`, `common_xieli`, `common_tuna`, `common_qingshen`, `common_feishi`, `common_zhuiying`, plus `common_mirror_armor`.
3. Wave 10 character identity bitmap pass: `cai_broken_string`, `cai_clear_tone`, `cai_echoing_melody`, `diao_hongyan`, `diao_falling_fan`, `zhao_guardian`, `zhao_stable_formation`, `zhuge_deduction`, `zhuge_plan_set`, and the rest of the Wave 10 Zhao/Diao/Cai/Zhuge set.
4. Wave 10 ink and mind bitmap pass: `ink_modian`, `ink_moren`, `ink_heiyu`, `mind_jingxin`, `mind_nuzhan`, `mind_luanxin`, `status_rain_chill`.

Special shared-asset cleanup:

- `zhao_strike` now uses Wave 9 repo-local semantic SVG art. `zhao_qixing_spear` now uses Wave 10 repo-local semantic SVG art and no longer shares the attack fallback image.

## First-Chapter Elite Regeneration Debt

The observed-bugfix branch quarantined known annotated/marked generated outputs instead of editing binary artifacts in place. Wave 9 replaced the earlier standee-only attack feedback with semantic SVG attack strips so first-chapter stand-ins no longer use the generic slash strip at runtime.

Wave 9 Task 3 replaced the standee-only attack feedback with semantic repo-local SVG strips. These are not GPT Image 2 bitmap regenerations, so bespoke GPT Image 2 standee/strip regeneration remains an art-quality backlog, but the runtime no longer falls back to generic slash or quarantined annotated attack PNGs for the three first-chapter stand-ins.

| Enemy | Current standee binding | Current attack binding | Regeneration target |
|---|---|---|---|
| `elite_sword_echo` / 剑痴残影 | `/assets/generated/gpt2-bamboo-soldier-standee-cutout.png` vetted stand-in | `/assets/sprites/wave9-sword-echo-attack-strip.svg` semantic Wave 9 SVG | Generate clean Sword Echo standee and GPT Image 2 four-frame attack strip when art quality backlog resumes |
| `elite_blood_banner` / 血旗都尉 | `/assets/generated/gpt2-scribe-officer-standee-cutout.png` vetted stand-in | `/assets/sprites/wave9-blood-banner-attack-strip.svg` semantic Wave 9 SVG | Generate clean Blood Banner standee and GPT Image 2 four-frame attack strip when art quality backlog resumes |
| `boss_ink_dongzhuo` / 墨影董卓 | `/assets/generated/gpt2-ink-dongzhuo-boss-standee-cutout.png` vetted boss standee | `/assets/sprites/wave9-ink-dongzhuo-boss-attack-strip.svg` semantic Wave 9 SVG | Generate clean Dong Zhuo boss GPT Image 2 four-frame attack strip when art quality backlog resumes |

Do not rebind the quarantined annotated paths without regenerated clean sources:

- `/assets/generated/sword-echo-standee-gpt-v2-cutout.png`
- `/assets/generated/blood-banner-standee-gpt-v2-cutout.png`
- `/assets/sprites/sword-echo-attack-strip-gpt-v2.png`
- `/assets/sprites/blood-banner-attack-strip-gpt-v2.png`
- `/assets/sprites/ink-dongzhuo-boss-attack-strip-gpt-v2.png`

Do not use `/assets/sprites/enemy-slash-strip.svg` as a runtime combatant sprite for these stand-ins. User playtest feedback showed the generic silhouette reads as a different enemy during attacks. The generic slash strip is not an acceptable runtime binding for `elite_sword_echo`, `elite_blood_banner`, or `boss_ink_dongzhuo`; use the semantic Wave 9 SVG strips until future clean GPT Image 2 strips replace them.

## Priority Targets

The prompt queue preserves the historical semantic replacement requests and the deferred card-face targets for:

- Cai Wenji standee, combat strip, and visible card faces.
- Zhuge Liang standee, formation strip, and visible card faces.
- Final Boss `无名史官` standee and combat strip.
- Final battlefield `墨渊照心`.
- Final chapter card/event faces for `未写之页` and `本心之问` / `照心水镜` style surfaces.
- Starter/common/filler card faces that now have Wave 9 or Wave 10 semantic SVG runtime coverage and can later be upgraded to GPT Image 2 bitmap art.
- Clean bespoke first-chapter elite standees and attack strips.

Every target should include:

- `type`
- `semanticId`
- `destinationPath`
- `sourcePrompt`
- `negativePrompt`
- `cropAlphaSequenceInstructions`
- `verificationCommand`

## Execution Notes

For the later generation wave:

1. Generate source sheets or individual source images from the queue prompts.
2. Save untouched sources under `public/assets/generated/sources/`.
3. Crop runtime files exactly to each target `destinationPath`.
4. Update `src/game/content/visuals.ts` to bind semantic ids to the new runtime paths.
5. Run:

```bash
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe scripts/audit-generated-assets.mjs
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe ./node_modules/vitest/vitest.mjs run tests/data/content.test.ts
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe ./node_modules/@playwright/test/cli.js test tests/e2e/visual-smoke.spec.ts
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe ./node_modules/vitest/vitest.mjs run
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe ./node_modules/vite/bin/vite.js build
```

The audit ledger must keep `missing` empty. The prompt queue may outlive generation as a historical handoff, but generated assets should shrink `cardFallbackDebt` and keep `inkPassDebt` at 0 rather than hide fallback usage.
