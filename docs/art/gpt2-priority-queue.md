# GPT Image 2 Priority Queue

Generated by Wave 3C on 2026-05-03 and refreshed by Wave 5, Wave 6 CardArtQueue rescue, Wave 7 alpha-doc refresh, Wave 8 release handoff, and the Wave 9 release-prep docs branch. This is a prompt and verification queue only; this refresh did not generate or bind new runtime images.

## Wave 9 Release Prep Targets

Wave 9 integration is expected to refresh the art ledger after the starter semantic card art and first-chapter semantic attack strip branches land:

- Starter semantic card art should reduce type-level fallback usage for the starter readability batch.
- First-chapter semantic attack strips should give `elite_sword_echo`, `elite_blood_banner`, and `boss_ink_dongzhuo` distinct runtime attack identity instead of the generic enemy slash.
- Final runtime reference totals, missing-file count, `ink-pass` debt, GPT2/source-sheet counts, prompt target counts, and card fallback debt are `集成后刷新`.

Do not report lower Wave 9 art debt until `scripts/audit-generated-assets.mjs` proves it on the integration branch.

## Baseline Audit

```bash
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe scripts/audit-generated-assets.mjs
```

Wave 9 integration result:

- Runtime references: `集成后刷新`
- Missing files: `集成后刷新`
- Current `ink-pass` debt: `集成后刷新`
- Non-blocking card fallback debt: `集成后刷新`

Audit must be rerun in the Wave 9 integration branch after balance, starter semantic card art, first-chapter semantic attack strips, and release docs are merged.

## Card Fallback Ledger

`cardFallbackDebt` remains intentionally non-blocking until dedicated semantic runtime art is generated or added and bound through `src/game/content/visuals.ts`.

Wave 6 CardArtQueue rescue status:

- No generated source sheet or runtime crop exists in this worktree for the starter readability or common foundation card-art batches.
- Milestone 58 remains deferred as art backlog rather than hidden by binding fallback files to card ids.
- The executable JSON queue keeps historical prompt targets and deferred card-face targets for starter/common card ids.
- Keep Wave 9 card fallback debt as `集成后刷新` until new semantic starter card art exists and the asset audit verifies the lower count.

The executable queue is stored at:

```text
public/assets/generated/gpt2-prompt-queue.json
```

Summary:

| Group | Count |
|---|---:|
| Total fallback cards | `集成后刷新` |
| Missing dedicated `cardArtById` entries | `集成后刷新` |
| Dedicated ids sharing type-level asset paths | `集成后刷新` |

By character:

| Character | Count |
|---|---:|
| common | `集成后刷新` |
| caiwenji | `集成后刷新` |
| zhaoyun | `集成后刷新` |
| zhugeliang | `集成后刷新` |
| diaochan | `集成后刷新` |

By type:

| Type | Count |
|---|---:|
| skill | `集成后刷新` |
| attack | `集成后刷新` |
| body | `集成后刷新` |

By rarity:

| Rarity | Count |
|---|---:|
| common | `集成后刷新` |
| starter | `集成后刷新` |
| uncommon | `集成后刷新` |
| rare | `集成后刷新` |
| ink | `集成后刷新` |
| status | `集成后刷新` |

Recommended next low-priority GPT Image 2 batches:

1. Starter readability batch: `zhao_guard`, `zhao_strike`, `zhao_longdan`, `diao_strike`, `diao_guard`, `diao_lingbo`, `cai_plain_strike`, `cai_pluck_string`, `cai_gong_tone`, `zhuge_fan_strike`, `zhuge_guard`.
2. Common foundation batch: `common_pifeng`, `common_duanzhu`, `common_gedang`, `common_xieli`, `common_tuna`, `common_qingshen`, `common_feishi`, `common_zhuiying`.
3. Character identity filler batch: `cai_broken_string`, `cai_clear_tone`, `cai_echoing_melody`, `diao_hongyan`, `diao_falling_fan`, `zhao_guardian`, `zhao_stable_formation`, `zhuge_deduction`, `zhuge_plan_set`.
4. Ink and mind cleanup batch: `ink_modian`, `ink_moren`, `ink_heiyu`, `mind_jingxin`, `mind_nuzhan`, `mind_luanxin`, `status_rain_chill`.

Special shared-asset cleanup:

- `zhao_strike` and `zhao_qixing_spear` already have direct `cardArtById` entries, but both still point at the shared attack fallback image. Give them semantic GPT Image 2 card faces before removing them from the ledger.

## First-Chapter Elite Regeneration Debt

The observed-bugfix branch quarantined known annotated/marked generated outputs instead of editing binary artifacts in place. Wave 9 should replace the first-chapter standee-only attack feedback with semantic attack strips after audit verification. Final runtime binding evidence is `集成后刷新`.

| Enemy | Current standee binding | Wave 9 attack target | Regeneration target |
|---|---|---|---|
| `elite_sword_echo` / 剑痴残影 | `/assets/generated/gpt2-bamboo-soldier-standee-cutout.png` vetted stand-in | Semantic Wave 9 strip; final path `集成后刷新` | Generate clean Sword Echo standee and four-frame attack strip, then bind semantic paths only after audit passes |
| `elite_blood_banner` / 血旗都尉 | `/assets/generated/gpt2-scribe-officer-standee-cutout.png` vetted stand-in | Semantic Wave 9 strip; final path `集成后刷新` | Generate clean Blood Banner standee and four-frame attack strip, then bind semantic paths only after audit passes |
| `boss_ink_dongzhuo` / 墨影董卓 | `/assets/generated/gpt2-ink-dongzhuo-boss-standee-cutout.png` vetted boss standee | Semantic Wave 9 strip; final path `集成后刷新` | Generate clean Dong Zhuo boss four-frame attack strip, then bind semantic path only after audit passes |

Do not rebind the quarantined annotated paths without regenerated clean sources:

- `/assets/generated/sword-echo-standee-gpt-v2-cutout.png`
- `/assets/generated/blood-banner-standee-gpt-v2-cutout.png`
- `/assets/sprites/sword-echo-attack-strip-gpt-v2.png`
- `/assets/sprites/blood-banner-attack-strip-gpt-v2.png`
- `/assets/sprites/ink-dongzhuo-boss-attack-strip-gpt-v2.png`

Do not use `/assets/sprites/enemy-slash-strip.svg` as a runtime combatant sprite for these stand-ins. User playtest feedback showed the generic silhouette reads as a different enemy during attacks, so these enemies must keep standee-only attack feedback until bespoke clean strips exist or the Wave 9 semantic strips are verified. The generic slash strip is not an acceptable runtime binding for `elite_sword_echo`, `elite_blood_banner`, or `boss_ink_dongzhuo`.

## Priority Targets

The prompt queue preserves the historical semantic replacement requests and the deferred card-face targets for:

- Cai Wenji standee, combat strip, and visible card faces.
- Zhuge Liang standee, formation strip, and visible card faces.
- Final Boss `无名史官` standee and combat strip.
- Final battlefield `墨渊照心`.
- Final chapter card/event faces for `未写之页` and `本心之问` / `照心水镜` style surfaces.
- Starter/common/filler card faces listed in the card fallback ledger.
- Clean bespoke first-chapter elite standees and attack strips.

Every target should include:

- `type`
- `semanticId`
- `destinationPath`
- `sourcePrompt`
- `negativePrompt`
- `cropAlphaSequenceInstructions`
- `verificationCommand`

## Execution Notes

For the later generation wave:

1. Generate source sheets or individual source images from the queue prompts.
2. Save untouched sources under `public/assets/generated/sources/`.
3. Crop runtime files exactly to each target `destinationPath`.
4. Update `src/game/content/visuals.ts` to bind semantic ids to the new runtime paths.
5. Run:

```bash
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe scripts/audit-generated-assets.mjs
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe ./node_modules/vitest/vitest.mjs run tests/data/content.test.ts
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe ./node_modules/@playwright/test/cli.js test tests/e2e/visual-smoke.spec.ts
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe ./node_modules/vitest/vitest.mjs run
/mnt/c/Users/loseheart/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe ./node_modules/vite/bin/vite.js build
```

The audit ledger must keep `missing` empty. The prompt queue may outlive generation as a historical handoff, but generated assets should shrink `cardFallbackDebt` and keep `inkPassDebt` at 0 rather than hide fallback usage.
