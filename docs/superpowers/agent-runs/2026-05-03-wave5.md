# 2026-05-03 Wave 5 Agent Run

## Base

- Main branch: `codex/next-major-modules`
- Base commit: `414e5bc docs: record wave four acceptance`
- Design: `docs/superpowers/specs/2026-05-03-wave5-post-mvp-polish-design.md`
- Plan: `docs/superpowers/plans/2026-05-03-wave5-post-mvp-polish.md`
- Goal: continue autonomous desktop MVP polish after Wave 4 acceptance.

## Planned Worktrees

| Agent | Branch | Worktree | Scope |
|---|---|---|---|
| Integrated | `codex/wave5-battlefields` | `.worktrees/wave5-battlefields` | Dynamic chapter battlefields |
| Integrated | `codex/wave5-final-route` | `.worktrees/wave5-final-route` | Browser-playable final boss route |
| Integrated | `codex/wave5-settings-audio` | `.worktrees/wave5-settings-audio` | Persisted settings and procedural feedback |
| Integrated | `codex/wave5-card-art-ledger` | `.worktrees/wave5-card-art-ledger` | Low-priority card fallback audit queue |

## Integration Rule

1. Keep Phaser battlefield adaptation separate from run/final-route gameplay.
2. Keep settings persistence and audio feedback separate from save/run semantics except where a typed settings storage helper is needed.
3. Keep card-art fallback auditing non-blocking: missing runtime assets remain blocking, shared fallback art remains a backlog.
4. Integrate in the order listed in the Wave 5 plan and reclaim each worktree after verification.

## Integration Results

| Scope | Integrated Commit | Verification | Worktree |
|---|---|---|---|
| Dynamic chapter battlefields | `f47d91c feat: add dynamic chapter battlefields` | `npm test -- tests/data/content.test.ts`; `npm run test:e2e -- tests/e2e/visual-smoke.spec.ts`; `npm test`; `npm run build` | Reclaimed |
| Browser final boss route | `d4e3e3a feat: add browser final boss route` | `npm test -- tests/run/run-system.test.ts tests/endings/ending-system.test.ts`; `npm run test:e2e -- tests/e2e/playable-flow.spec.ts --grep "final boss route"`; `npm test`; `npm run build` | Reclaimed |
| Persisted settings and procedural feedback | `99eee21 feat: persist settings and audio feedback` | `npm test -- tests/app-shell.test.ts tests/save/save-system.test.ts tests/app-settings.test.ts`; `npm run test:e2e -- tests/e2e/playable-flow.spec.ts --grep "settings"`; `npm test`; `npm run build` | Reclaimed |
| Card art fallback ledger | `5c09daf test: add card art fallback ledger` | `node scripts/audit-generated-assets.mjs`; `npm test -- tests/data/content.test.ts`; `npm test`; `npm run build` | Reclaimed |

## Final Gate Plan

Run on `codex/next-major-modules` after documentation is updated:

```bash
node scripts/audit-generated-assets.mjs
npm test
npm run typecheck
npm run build
npm run test:e2e
```

Expected current audit baseline:

- Runtime references: 105
- Missing files: 0
- Ink-pass debt: 0
- Non-blocking card fallback debt: 56
- GPT Image 2 runtime assets: 55
- Source sheets: 20
- Prompt queue targets: 35

Notes:

- The dynamic battlefield, final-route, and settings/audio workers became unavailable after session compaction, so their worktrees were reviewed, verified, integrated, and removed manually from the coordinator session.
- The card-art ledger worker also became unavailable after writing its worktree changes; the coordinator verified the worktree independently before committing and integrating it.
