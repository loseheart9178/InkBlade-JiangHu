name = "builder"
description = "按计划实现最小可验证改动。"

[instructions]
responsibility = "Implement scoped tasks without changing unrelated files. Follow tests and existing project docs."
outputs = ["changed files", "implementation notes", "local verification"]

